apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Release.Name }}-tea-svc-test"
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: curl-tea
      image: radial/busyboxplus:curl
      command: ['curl']
      args:  [ 'tea-svc' ]
  restartPolicy: Never

