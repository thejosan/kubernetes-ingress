apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Release.Name }}-coffee-svc-test"
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: curl-coffee
      image: radial/busyboxplus:curl
      command: ['curl']
      args:  [ 'coffee-svc' ]
  restartPolicy: Never

